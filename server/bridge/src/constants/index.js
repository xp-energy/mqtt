const TOKEN_EXPIRE_MIN = 2 * 60; // Five minutes
const TO_MS = 1e3;
const PROJECT_ID = `xperesidencial`;
const REGION = `us-central1`;
const REGISTRY_ID = `measurementsNode`;
const ALGORITHM = `RS256`;

module.exports = {
  TOKEN_EXPIRE_MIN,
  PROJECT_ID,
  REGION,
  REGISTRY_ID,
  ALGORITHM,
  TO_MS,
};
